imports:
    - { resource: config.yml }

assetic:
    use_controller: false

framework:
    router:   { resource: "%kernel.root_dir%/config/routing_dev.yml" }
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
        firephp:
            type:  firephp
            level: info

assetic:
    use_controller: true

bazinga_faker:
    orm: doctrine

swiftmailer:
    disable_delivery: true

parameters:
    payment.gateway.api_key: "sk_test_fXB6jN3wWpxC1u2BfBAUWw7L"
